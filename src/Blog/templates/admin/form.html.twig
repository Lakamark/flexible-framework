{{ field('name', item.name, "Title") }}
{{ field('slug', item.slug, "Link") }}
{{ field('content', item.content, "Content", {type: 'textarea'}) }}
{{ field('created_at', item.created_at, "Published At", {class: 'datepicker'}) }}